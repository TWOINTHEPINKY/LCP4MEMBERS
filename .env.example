# Public frontend configuration; Vite includes VITE_* values in builds. Never put secrets here.
# API URL is required for production builds (use /api for same-origin production).
VITE_API_URL=http://localhost:8000
# Public support account. Set https://t.me/<username>; empty/invalid disables the action.
VITE_SUPPORT_TELEGRAM_URL=
