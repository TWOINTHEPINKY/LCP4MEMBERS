BOT_TOKEN=
# Generate two DIFFERENT random secrets (at least 32 characters each).
JWT_SECRET=
# Must match bot/.env; never use BOT_TOKEN as this secret.
BOT_INTERNAL_SECRET=
BOT_USERNAME=connorsvpn_bot
